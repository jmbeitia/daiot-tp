# Túnel ngrok para docker-compose.dev.yml
# Authtoken de tu cuenta ngrok (Dashboard > Your Authtoken)
NGROK_AUTHTOKEN="tu_authtoken_aqui"

# Dominio estático reservado en ngrok (Dashboard > Domains)
NGROK_DOMAIN="tu-dominio.ngrok-free.dev"
